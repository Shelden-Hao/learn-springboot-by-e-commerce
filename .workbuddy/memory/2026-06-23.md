# 2026-06-23 工作日志

## Task 10 收尾 — Admin 角色删除权限
- 创建 UserRole 枚举（USER/ADMIN）
- schema.sql 加 role 列 + 重建 Docker 容器
- AuthController.login() 存 role.name() 到 Sa-Token Session
- ProductController.delete() 加 @SaCheckRole("ADMIN")
- 创建 StpInterfaceImpl 实现角色查询
- SaTokenConfig 移除 /api/products/** 排除，改用注解方式
- 验证：admin 删 204、test 删 403

## Task 11 — 定时任务自动取消过期订单
- 建 SchedulingConfig（@EnableScheduling）
- 建 ScheduledTaskConfig（@Scheduled + @Transactional）
- LambdaQueryWrapper 查 PENDING + createdAt < 5分钟前
- 取消 + 恢复库存
- 泛型推断 bug：LambdaQueryWrapper 嵌套在方法参数里需单独声明变量

## Task 12 — 订单分页查询
- OrderController.list() 改用 Page + LambdaQueryWrapper
- 支持 buyerName 模糊搜索 + status 枚举筛选
- valueOf bug：用了 String 变量而非 OrderStatus 枚举

## Task B — CSV 订单导出
- OrderController.export() 直接写 OutputStream
- Content-Disposition: attachment 触发浏览器下载
- UTF-8 BOM 解决 Excel 中文乱码
- String.join() 拼接 CSV 行

## Task D — 图片上传
- MultipartFile 接收文件
- UUID 生成唯一文件名
- Paths.get().toAbsolutePath() 处理相对路径陷阱
- WebConfig 静态资源映射 /uploads/** → ./uploads/

## Task F — 多环境配置
- 拆分 application.yaml → application-dev.yaml + application-prod.yaml
- spring.profiles.active: dev
- 数据库名 bug：dev 写了 demo 而非 demo_db

## Task G — AOP 请求日志
- 建 RequestLogAspect（@Aspect + @Around）
- 拦截 controller 包下所有方法
- 打印 HTTP method + path + 耗时
- 验证：GET /api/products 第一次 82ms，第二次 11ms（缓存命中）

## README
- 完成项目 README.md，记录从零到学完的全过程
